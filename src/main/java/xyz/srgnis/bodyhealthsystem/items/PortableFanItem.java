package xyz.srgnis.bodyhealthsystem.items;

import net.minecraft.item.Item;

/**
 * Placeholder Portable Fan item. Rendering is handled by its JSON model.
 * Further behavior (cooling, toggles, etc.) can be added later.
 */
public class PortableFanItem extends Item {
    public PortableFanItem(Settings settings) {
        super(settings);
    }
}
